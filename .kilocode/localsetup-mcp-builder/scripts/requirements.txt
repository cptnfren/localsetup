# MCP connection (required for all evaluation modes)
mcp>=1.1.0

# Claude provider (--provider claude)
anthropic>=0.39.0

# OpenAI-compatible provider (--provider openai)
openai>=1.0.0

# Emulation (--provider emulation) requires no extra packages; only mcp above for MCP connection.
