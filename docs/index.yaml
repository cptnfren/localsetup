#
# Machine index for documentation.
# This file is the source of truth for docs metadata.
# Agents should update or insert entries here and regenerate docs/INDEX.md from it.
#
# Schema for each entry:
# - id: stable identifier (slug or UUID), must remain stable even if path changes
# - path: repo relative path to the markdown file
# - title: human title
# - category_label: high level category such as runbook, architecture, spec, how_to, notes, reference
# - category_slug: folder safe slug for the category
# - component_slug: optional slug for the primary system or feature
# - status: lifecycle status from DOCUMENT_LIFECYCLE_MANAGEMENT.md (for example ACTIVE, PROPOSAL, DEPRECATED)
# - last_updated: ISO date string (YYYY-MM-DD)
# - doc_type: coarse type, usually close to doc_type_hint
# - tags: list of short strings
# - nonstandard_location: optional boolean, true when the chosen path does not match the recommended placement
#

entries:
  - id: "docs-incident-runbooks-payments-api-key-rotation"
    path: "docs/incident-runbooks/payments/payments-api-key-rotation.md"
    title: "Payments API key rotation runbook"
    category_label: "runbook"
    category_slug: "incident-runbooks"
    component_slug: "payments"
    status: "ACTIVE"
    last_updated: "2026-03-09"
    doc_type: "runbook"
    tags:
      - "payments"
      - "api-keys"
      - "runbook"
    nonstandard_location: false

# Machine readable documentation index.
# This file is owned by the docs organization skill.
# Do not edit this file by hand. Use the docs organization skill when
# creating or updating docs so it can keep this index in sync with the file tree.

