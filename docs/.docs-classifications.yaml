#
# Repo local manifest of known documentation categories.
# This file is owned by the repo and may be extended over time.
#
# Each entry has:
# - label: human facing label
# - slug: folder safe slug
# - description: short explanation of how this category is used here
#

categories:
  - label: "runbook"
    slug: "incident-runbooks"
    description: "Incident response and operational runbooks, often scoped to a single service or subsystem."
  - label: "architecture"
    slug: "architecture"
    description: "Architecture overviews and design level documentation for systems and services."

# Repo local classification manifest for documentation.
# Entries in this file record category labels and their slugs so agents can
# reuse existing categories instead of inventing new ones for similar docs.
#
# Example shape:
# - label: "architecture"
#   slug: "architecture"
#   description: "High level or component level architecture overviews."
# - label: "runbook"
#   slug: "runbooks"
#   description: "Operational runbooks and on call procedures."
#
# The docs organization skill updates this file as new categories emerge.
# You can seed it with a few categories if your repo already has a structure,
# but avoid adding very specific or one off labels here.

[]

