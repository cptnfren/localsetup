---
status: ACTIVE
version: 2.10
---

# PRD schema and external agent guide (Localsetup v2)

**Purpose:** PRD/spec format, front matter, lifecycle, outcome template. When referencing repo artifacts, attach git hash (see GIT_TRACEABILITY.md).

## Spec format

- **Front matter:** status (ready | in-progress | done | blocked), priority, optional external_confirmation, impact_review.
- **Sections:** Implementation steps, Acceptance criteria, Verification plan, Rollback plan.
- **Outcome block:** Append per spec (commit SHA, files changed, verification, rollback command).

## Field-to-queue/protocol map

Use this table to see where each front-matter field is interpreted. Queue layout and batch behavior live in [AGENTIC_AGENT_Q_PATTERN.md](AGENTIC_AGENT_Q_PATTERN.md); agent-to-agent transport behavior lives in [AGENTIC_AGENT_TO_AGENT_PROTOCOL.md](AGENTIC_AGENT_TO_AGENT_PROTOCOL.md).

| Field | Type | Where it matters | Notes |
|-------|------|------------------|-------|
| `status` | enum | Queue pattern, batch implementer | Drives which PRDs are picked (`ready` / `in-progress` / `done` / `blocked`). |
| `priority` | int/enum | Queue pattern, batch implementer | Used for sort order inside the queue. |
| `external_confirmation` | string/enum | Batch implementer, guardrails | When acknowledged, can satisfy impact_review gates per WORKFLOW_REGISTRY guidance. |
| `impact_review` | string/enum | Batch implementer, guardrails | Signals whether big/destructive changes require explicit PHC. |
| `localsetup_framework_version` | string | Protocol, batch implementer | Version stamp; mismatches may warn or block per protocol/config. |
| `localsetup_framework_hash` | string (sha) | Traceability only | Optional; see [GIT_TRACEABILITY.md](GIT_TRACEABILITY.md). |
| `from_agent` / `to_agent` | string | Agent-to-agent context | Logical agent ids for handoff; protocol uses these when wrapping PRDs for transport. |
| `ack_required` | bool | Protocol, queue `pending/` | When true, protocol may require explicit ack and queue may move items under `pending/`. |
| `delivery` | enum (`mail` \| `manual`) | Protocol, transport client | Per-request delivery hint; interpreted by the transport client, not by the batch itself. |
| `deliverable` | enum (e.g. `tar_build` \| `zip_build` \| `branch_only` \| `outcome_only`) | Protocol, transport client | Describes what B should expect on receive; outer envelope and manifest reflect this. |
| `conversation_id` | string | Protocol, archive layout | Stable across iterations; used for archive path under `archive/<conversation_id>/`. |
| `iteration` | integer | Protocol, archive layout | Monotonic per conversation; combined with conversation_id in outcomes. |
| `transport` | enum (`mail` \| `file_drop` \| `manual`) | Protocol, operator | Optional override; actual behavior is enforced by transport client + registry, not batch. |
| `pre_ship_checks` | list[string] | Protocol, pre-ship gate | Commands to run before ship; results are recorded in outcome when using agent-to-agent flows. |
| `supersedes_message_id` / `base_iteration` | string / integer | Protocol, iteration/backlog | Indicates that this PRD replaces or continues a prior one in the same conversation. |

This table describes **where** the fields are consumed; the **shape** (types, allowed values) is defined here in the PRD schema and must stay consistent with the protocol and queue docs.

## Tool-generated and agent-to-agent front matter (optional)

When tooling writes or transports a PRD, the **semantics** of these fields follow [AGENTIC_AGENT_TO_AGENT_PROTOCOL.md](AGENTIC_AGENT_TO_AGENT_PROTOCOL.md). This guide defines the shape; the protocol defines how transports and queues interpret them.

- **localsetup_framework_version** - String from repo VERSION at write time (recommended when generated by tooling).
- **localsetup_framework_hash** - Optional git sha for traceability.
- **from_agent** / **to_agent** - Optional agent ids for handoff.
- **ack_required** - If true, B should ack before or when starting.
- **delivery** - `mail` | `manual` (per request).
- **deliverable** - e.g. `tar_build` | `zip_build` | `branch_only` | `outcome_only`.
- **conversation_id** - Stable across iteration rounds.
- **iteration** - Integer, monotonic per conversation when iterating.
- **transport** - Optional override `mail` | `file_drop` | `manual`.
- **pre_ship_checks** - Optional list of commands to run before ship; exit codes recorded in outcome.

Supersede / conflict:

- **supersedes_message_id** or **base_iteration** when replacing prior work.
- **status: superseded** on obsolete PRD when applicable.

## Outcome block extensions (agent-to-agent)

When shipping to another agent via mail or file_drop:

- **sent_to_agent**, **message_id** (mail) or **transport_ref** (file_drop).
- **artifact_filename**, **manifest_filename**, **checksum_algorithm** as needed.
- **conversation_id**, **iteration_shipped**, **archive_path** or **archive_manifest** for B archive / unfold-debug.
- **skip_pre_ship_checks** + reason only in emergencies.

## Queue and batch

- Specs in `.agent/queue/` (or configured path). Exclude README, INDEX, SPEC-TEMPLATE.
- Filter by status; sort by priority then filename. Implement per spec; update status; write outcome.
- **Clean-tree:** Before marking done, repo clean (commit or revert). See skill localsetup-agentic-prd-batch.
- **Version mismatch:** If `localsetup_framework_version` differs from local VERSION, surface warning per protocol (do not silently ignore).
- **Pre-ship:** When `to_agent` and deliverable ship, pre-ship gate must pass or skip documented in outcome.

## How PRDs interact with bidirectional Agent Q

At a high level:

- **PRD shape and lifecycle** come from this guide: front matter + sections + outcome block describe what to do and how to record results.
- **Queue pattern** in [AGENTIC_AGENT_Q_PATTERN.md](AGENTIC_AGENT_Q_PATTERN.md) decides **where** PRDs live on disk (`.agent/queue`, optional `in/`, `inbox/`, `out/`, `pending/`, `archive/`) and how batch processing walks them.
- **Agent-to-agent protocol** in [AGENTIC_AGENT_TO_AGENT_PROTOCOL.md](AGENTIC_AGENT_TO_AGENT_PROTOCOL.md) and scenarios in [AGENTIC_AGENT_Q_SCENARIOS.md](AGENTIC_AGENT_Q_SCENARIOS.md) define **how** a PRD and its artifacts move between agents (OpenPGP envelopes, registry, file_drop/mail adapters, ack and iteration handling).

When you want a PRD to participate in a bidirectional Agent Q flow:

1. Write the spec using this schema (front matter + sections + outcome block).
2. Place it under the configured queue root (flat or structured) so the batch can see it.
3. Use the transport client (see `_localsetup/tools/agentq_transport_client/docs/USER_GUIDE.md`) to ship or ingest sealed blobs; the client and protocol docs interpret fields like `ack_required`, `delivery`, `deliverable`, `conversation_id`, and `iteration` according to AGENTIC_AGENT_TO_AGENT_PROTOCOL.

## External confirmation

- If spec has external_confirmation acknowledged or impact_review confirmed by external agent, implementer may skip human impact confirmation; otherwise follow guardrails (impact summary + user confirmation for big/destructive).
- **Iteration does not bypass** PHC gates; iteration PRDs keep impact_review as needed.

## Related

- [AGENTIC_AGENT_TO_AGENT_PROTOCOL.md](AGENTIC_AGENT_TO_AGENT_PROTOCOL.md) - Agent-to-agent transport protocol (ACTIVE).
- [AGENTIC_AGENT_Q_BIDIRECTIONAL_BUILD_SPEC.md](AGENTIC_AGENT_Q_BIDIRECTIONAL_BUILD_SPEC.md) - Build order and conventions.
- GIT_TRACEABILITY.md
- WORKFLOW_REGISTRY.md

---

<p align="center">
<strong>Author:</strong> <a href="https://github.com/cptnfren">Slavic Kozyuk</a><br>
<strong>Copyright</strong> 2026 <a href="https://www.cruxexperts.com/">Crux Experts LLC</a> – Innovate, Automate, Dominate.
</p>
